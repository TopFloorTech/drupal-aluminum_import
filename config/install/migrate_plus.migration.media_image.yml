id: media_image
label: Migrate a media entity for an image field
migration_group: shared
source:
  plugin: aluminum_import_csv_media_entity
destination:
  plugin: entity:media
process:
  type:
    plugin: default_value
    default_value: image
  uid:
    plugin: default_value
    default_value: 1
  field_image:
    plugin: migration
    migration: file_image
    source: field_image

migration_dependencies:
  required:
    - file_image

dependencies:
  enforced:
    module:
      - aluminum_import
